// Animação para a seção de início
ScrollReveal().reveal('#inicio', {
  delay: 500,
  duration: 1000,
  origin: 'bottom',
  distance: '50px',
  reset: true
});

// Animação para a seção "sobre"
ScrollReveal().reveal('#sobre', {
  delay: 400,
  duration: 1000,
  origin: 'left',
  distance: '60px',
  reset: true
});

// Animação para os cards de serviços
ScrollReveal().reveal('.card-servico', {
  delay: 300,
  duration: 800,
  origin: 'right',
  distance: '40px',
  interval: 200,
  reset: true
});

// Animação para a seção de contato
ScrollReveal().reveal('#contato', {
  delay: 600,
  duration: 1000,
  origin: 'top',
  distance: '70px',
  reset: true
});
// Animação para a seção de início
ScrollReveal().reveal('#inicio', {
  delay: 500,
  duration: 1000,
  origin: 'bottom',
  distance: '50px',
  reset: true
});

// Animação para a seção "sobre"
ScrollReveal().reveal('#sobre', {
  delay: 400,
  duration: 1000,
  origin: 'left',
  distance: '60px',
  reset: true
});

// Animação para os cards de serviços
ScrollReveal().reveal('.card-servico', {
  delay: 300,
  duration: 800,
  origin: 'right',
  distance: '40px',
  interval: 200,
  reset: true
});

// Animação para a seção de contato
ScrollReveal().reveal('#contato', {
  delay: 600,
  duration: 1000,
  origin: 'top',
  distance: '70px',
  reset: true
});

